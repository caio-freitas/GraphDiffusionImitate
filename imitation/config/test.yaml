defaults:
  - _self_
  - task: lift_lowdim
  - policy: robomimic_lowdim

render: True
output_video: False

seed: 0
num_episodes: 5
max_steps: 500
output_dir: ./outputs

pred_horizon: 1
obs_horizon: 1
action_horizon: 1


agent:
  _target_: imitation.agent.robomimic_lowdim_agent.RobomimicLowdimAgent
  env: ${task.env_runner}